h3 = @survey.title

.added_companies
  p 公開済み法人
  - @added_companies.each do |company|
    li
      = company.name
.publication_setting 公開する
= form_for [@survey, @surveys_company] do |f|
  -@companies.each do |company|
    = f.check_box :company_ids ,{ multiple: true, checked: company[:checked], include_hidden: false}, company.id
    = company.name
  = f.submit
